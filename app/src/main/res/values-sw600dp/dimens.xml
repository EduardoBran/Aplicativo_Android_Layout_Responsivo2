<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Toolbar -->
    <dimen name="toolbar_title_text_size">28sp</dimen>

    <!-- margem geral do bloco de conteúdo (ConstraintLayout interno) -->
    <dimen name="content_margin">16dp</dimen>

    <!-- margens horizontais das imagens -->
    <dimen name="image_margin_start">20dp</dimen>
    <dimen name="image_margin_end">20dp</dimen>

    <!-- margen top das imagens -->
    <dimen name="margin_top_images">24dp</dimen>

    <!-- tamanho (largura/altura) das imagens principais -->
    <dimen name="main_image_size">270dp</dimen>

    <!-- tamanho da View que mostra o curso popular -->
    <dimen name="course_view_height">88dp</dimen>

    <!-- tamanho (largura/altura) do ícone dentro da view de curso -->
    <dimen name="course_icon_size">56dp</dimen>

    <!-- tamanhos de texto -->
    <dimen name="text_size_hello">28sp</dimen>
    <dimen name="text_size_learn">32sp</dimen>
    <dimen name="text_size_popular_course">28sp</dimen>
    <dimen name="text_size_see_all">28sp</dimen>
    <dimen name="text_size_android_development">26sp</dimen>
</resources>